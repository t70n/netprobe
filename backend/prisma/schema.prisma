generator client {
   provider = "prisma-client-js"
}

model alarm {
   id			         Int		@default(autoincrement()) @id
   signal_id		   String		
   signal_label		String
}

datasource db {
   provider = "sqlite"
   url      = "file:./dev.db"
}
